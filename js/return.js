    // Scroll to the top of the page when it's loaded or when a link is clicked
    window.onload = function() {
        scrollToTop();
    };

    // Function to scroll to the top of the page
    function scrollToTop() {
        window.scrollTo(0, 0);
    }